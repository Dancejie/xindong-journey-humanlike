# R11 自定义玩家验证

时间：2026-09-09；当前版本：4.2.2-custom-video-availability-r11。

## 内嵌视频黑屏修复（当前，非尺寸问题）

- 用户进一步明确是「黑屏看不到视频」。在其原Codex标签页实际点击播放，看到进度从0:00走到0:10但整个视频区域始终黑色。上一轮仅放大层播放成功不能证明内嵌播放器正常；尺寸修复不是此问题的解决证据。
- 将唯一视频卡片 `.custom-video-panel.glass-card` 的标准和WebKit `backdrop-filter` 都关闭，保留卡片底色、其他UI毛玻璃与完整画幅。这避免内嵌浏览器视频与毛玻璃合成层冲突；不改生成片段、解码参数、人物资料或业务状态。
- Vite build通过，当前CSS `index-ULyUvBfs.css`。在实际Codex浏览器新打开 `?intro=1&qa=video-paint-r11`，从同一已保存角色直接播放inline视频（未放大），实见起始侧脸动作和0:10结束的正脸画面；已将此修复页面打开供用户使用。原标签页旧bundle尚不会自行刷新。
- 四尺寸回归全部通过、pageErrors空：新增所有inline祖先无backdrop-filter检查，以及真实媒体requestVideoFrameCallback、videoWidth/Height和非黑像素检查，不再只看进度或DOM布局。API mock无新生成，实际费用账本仍1项/预留¥2。
- 本地修复，未部署Render。原视频已存在且可解码，本轮不上传、不重生成、不收新生成费用。

## 完整画幅预览（2026-09-09后续反馈）

- 用户已反馈现有生成效果符合预期，保留该片段，不上传、不重生成。原inline已使用contain，但仅245px宽/42dvh高，观看过小；调整为容器宽/62dvh（最高560px），新增原片「放大查看完整视频」原生dialog。
- 放大层保持contain、居中完整画幅，关闭/原生播放控件可达；打开时暂停inline、加载后续接时间和播放状态，关闭/Escape暂停大播放器，不出现双音轨。只GET已有资源，不调用生成API，不修改人物或剧情。
- TS与Vite build通过。390×844/720/620和1280×800回归验证完整视频边界、关闭可达、Escape暂停、同一资源、零新增生成请求，保留原默认生成/幂等/照片授权回归。短屏mock截图目视通过。
- 在实际Codex本地浏览器读取原已保存角色，打开大预览并播放到0:10/0:10，看到完整竖屏人物，关闭控件可见。真实画幅审核沿用用户认可，未擅自批量批准其他人物；仅核对单个现有结果。临时QA标签关闭，原用户页面和资料未动。
- 真实费用账本检查为1项/预留¥2（用户之前已生成的一项），本轮不提交任务。当前入口仍是4184本地，未部署Render。
- 清理脚本dry-run/apply无目标、运行媒体hash校验通过；9张临时mock截图精确清理，回收1,241,897 bytes（约1.18MiB），保留紧凑测试报告，用户原图和生成片段未删除。

## 持续授权与本地启用（当前状态，取代下文历史关闭状态）

- 用户明确「以后这个链路无需确认」：限定Mini/480p/约10s自定义动态形象，新建默认一次提交、旧planned角色单次按钮无二次付费弹窗；照片授权与候选形象验收仍保留。范围记录于 `media/production/BUDGET_POLICY.md`，本地保守累计上限¥5、每次经验预留¥2，不自动补充额度，不扩大到其他生成链路。
- 修复LaunchAgent缺Homebrew PATH导致找不到已安装ffprobe的问题，保留全部视频技术校验。重启后实际4184 `/health` 200，`GET /api/custom-characters` 的generation返回 `available:true`、`autoSubmit:true`、`modelLabel:Seedance 2.0 Mini`、480p、10s、`estimatedCostCny:2.0`；不再显示通道未启用。
- 列表与每个人物详情读取同一项目账本，剩余不足一项费用时提前禁用生成；耗尽提示不再要求用户重复确认，已有照片和视频仍可用。提交时继续锁内检查，不因UI检查替代原子扣费保护。
- 188项后端测试通过，使用随机隔离PostgreSQL schema（已销毁）及mock provider。新增跨账号共用额度、余量不足一次、重复GET不扣费回归。390×844/720/620的23类mock浏览器断言通过，pageErrors为空；目视默认提交披露无重叠。Mac锁定使实际用户浏览器目视重放未执行，未修改用户浏览器资料；本地真实配置由只读接口核验。
- 实际业务库账本仍为0项/¥0：没有向供应商上传新照片，没有提交真实付费任务，也没有批量补交已保存人物。本轮仅本地启用，未提交/推送GitHub或部署Render，不声称生成完成或线上已同步。
- 既有Lite清理脚本dry-run及apply均为空目标、回收0 bytes，保留运行媒体及当前用于4184服务的dist；8张临时mock截图在记录判定后精确清理，回收1,228,657 bytes（约1.17MiB），保留紧凑JSON报告。

## Mini型号与不可用恢复修复（4.2.2）

- 用户明确「我指的就是2.0 mini」后，将误解成正式2.0的限制修正为精确 `seedance-2.0-mini`，全界面显示Mini/480p/10s，不伪称其他型号。
- 只读Fumin模型列表HTTP200：Mini、2.5；使用本项目已结算R9B任务只读恢复，确认Mini及精确下载域名。未上传新图片、未提交生成任务、未读取或展示签名URL/密钥。
- 187项后端测试通过，adapter33项含本地真实编码/ffprobe，新增接受已验证Mini480p的496×864，拒绝未知扩展尺寸。旧任务时长/型号仍取保存规格，不能被新默认覆盖。
- TS、Vite build与390×844/720/620 mock回归通过。未启用不再标「可选」；可点击免费GET检查通道，配置恢复后仅planned允许用户明确按价提交，失败/未知仍不重提。保存列表刷新同步当前角色，避免旧配置一直留在页面。
- 主agent目视短屏不可用面板：Mini与10秒可见，禁用原因和免费检查入口可见，无横溢；实际4184浏览器打开用户原已保存角色，确认显示原资料、Mini/10秒与唯一预算阻塞提示。照片和已有角色不删除、不重新上传。
- 本地LaunchAgent对原Documents凭据文件的读取限制导致启动失败；已将现有获准Fumin配置提供至git忽略的私有运行目录（700/600），不改系统权限，凭据读取异常改为只关闭视频而非退出游戏。重启后4184 health200、版本一致、Dots/DeepSeek保留。
- 实际生成尚未启用：R11新增累计预算未确认。最近同档结算只支持经验估算10秒约¥1.24；设置单次¥2保守预留不是供应商封顶报价，不挪用旧批预算。业务库新任务0项、预留¥0。没有线上部署或真实候选验收。
- Lite cleanup dry-run/apply核验运行资产hash且无待清理残留（0 bytes）；模拟QA临时截图已清理，主agent最后删除已看过的98,821-byte截图，紧凑report保留；无新增生成媒体。最终费用账本仍0项/¥0，临时浏览器验收页已关闭。

## 默认自动视频增量验收（4.2.1，历史记录）

- 创建默认携带 `autoVideo: true`，绑定已展示价格，一次性提交；取消勾选、旧客户端不传此字段、打开旧角色、重复创建请求、轮询都不触发新费用。
- 新任务锁定 Seedance 2.0 正式版 / 480p / 10s。Mini/Fast/2.5 在上传前拒绝；精确ID必须由通道models响应再次确认。不声称语法检查就证明通道可用。
- 186项后端测试通过（含隔离PostgreSQL测试），其中视频adapter32项：10s提交/spec与hash校验、旧5s任务恢复、真实本地5s/10s ffmpeg→ffprobe校验、未知提交不重试、安全下载。全部provider mock，无实际上传/付费生成。
- 自动创建新增检查：一次预留/一次提交、价格变化/未配置/预算不足仍201保存卡片、配置恢复后不自动补单、超时未知任务不重新收费。保留候选人工形象确认。创建后的卡片合并和预算预留放至worker线程，避免与持有行锁的异步poll互相等待；增加线程边界回归检查。
- TypeScript与Vite build通过。20类Mock浏览器断言通过，覆盖390×844/720/620：默认自动一次、取消静态、费用绑定、旧卡无补单、历史5s与旧型号正确标注、reserved不冒称已远端提交、候选确认、44px控件、无横溢；零页面错误。报告：`/tmp/heart-journey-custom-auto-ui/report.json`。
- 主agent在实际4184浏览器目视确认默认勾选「同时生成动态形象」、Seedance 2.0 / 480p / 约10s、配置缺失说明和照片开局入口；未触碰用户已保存角色或提交其照片。
- 本地服务重启后 `/health` 返回当前版本；`GET /api/custom-characters` 返回 `autoSubmit:true` 和10s规格，同时准确返回 `available:false`（本地正式2.0型号/凭据未配置）。实际业务库视频费用账本为0项、¥0。功能逻辑完成不等于真实通道已启用，不声称生成或上线。
- 清理：Lite dry-run/apply核验运行媒体hash且无待删文件（0 bytes）；移动端临时截图约2.26MB已按测试目录删除，5194测试服务停止。原始素材、既有角色与实际4184运行build保留。

## 初版人物功能验收（4.2.0，历史记录）

- 172项后端测试通过，包括32种MBTI×性别组合、自定义玩家8人阵容、请求并发隔离、第一天全流程、Agent/Director提示词、记忆、短信与媒体本人匹配。
- 使用显式本地PostgreSQL数据库的随机隔离schema测试：图片格式/大小/EXIF、18+授权、跨所有者拒绝、创建幂等、开局和恢复、能力URL读取/Range、删除后404、关联局清理、视频费用预留不可通过删除绕过、重复点击只提交一次、候选须确认才进入角色video。隔离schema自动删除。
- 前端TypeScript与Vite build通过。15类mock UI断言通过（16MBTI、照片校验、成年人授权、草稿保留、同ID重试、custom start、无自动付费、费用绑定、候选审核、删除确认、44px按钮、无横溢、无个人资料localStorage、双击防重复）。
- 真实浏览器→HTTP→本地PG回放通过：上传既有批准角色肖像作为测试输入→归一化照片→本人资料卡→custom开局→7位NPC→本人照片舞台→自聊禁用→重载恢复→已保存角色→删除关联数据并撤销媒体URL。
- 390×844、390×720、390×620无横溢，页内错误为空；主agent已目视真实照片预览与游戏截图。照片为项目既有批准素材，不是新增用户真人样本。
- 所有付费provider在隔离浏览器测试关闭；未配置模型时503与提示是明确的降级测试，不宣称真实LLM对话生成成功。
- 视频adapter 23项mock测试通过，包括精确型号/规格hash/参考hash/费用授权、一次提交、未知结果不重试、HTTPS与域名/DNS/重定向防护、24MB限额和ffprobe规格验证。未做真实付费生成，不能视为新视频身份或表演验收。

本地4184现有预览已更新并返回该版本health。现有DeepSeek/Dots配置保留；自定义视频通道默认未启用，需要当前明确预算与安全环境配置。未提交Git、未推送GitHub、未更新Render，既有线上版本保持不变。

资源清理：既有Lite清理脚本dry-run/--apply校验运行媒体hash，未发现旧轮次残留（0 bytes）。本轮11张临时浏览器截图在目视后按精确列表删除，回收1,969,675 bytes（约1.88MiB）；原图hash不变。临时4186/5194服务已停止，隔离浏览器schema在确认自定义人物为0后删除。保留源码测试、紧凑报告、既有原图及运行时文件，不新增生成候选媒体。
